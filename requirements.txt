# Market data comes from Alpaca's REST API, which answers JSON, so an HTTP
# client is the whole dependency. yfinance, pandas and numpy were all here
# to parse and carry price frames; nothing does that any more.
requests==2.34.2
psycopg2-binary==2.9.12
python-dotenv==1.2.2
# zoneinfo needs this on Windows, which ships no timezone database
tzdata==2023.3

# Web interface
Flask==3.1.3
Flask-WTF==1.2.2
Authlib==1.8.0
# Production server (used on Render; does not run on Windows, which is fine:
# local development uses `flask run`)
gunicorn==23.0.0

# AI assistant
google-genai==2.23.0

# Testing
pytest==9.1.1
